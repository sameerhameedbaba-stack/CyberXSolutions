node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*.local
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# Zipped copies of the export, for hand-uploading to a host. Build output, so
# regenerable — `npm run build:static && cd out && zip -r ../site.zip .`
*.zip
